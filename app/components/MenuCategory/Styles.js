import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  sectionHeader: {
    fontWeight: 'bold',
    fontSize: 25,
    marginBottom: 10,
    marginTop: 10,
  },
  itemView: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginTop: 10,
    marginBottom: 10,
  },
});
